<nav class="navbar navbar-expand-lg ">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('app_home')}}"><img style="width:50px;" src="{{asset('img/logo.png')}}" alt="logo"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{path('app_home')}}">Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item">
					<a class="nav-link active" href="{{path('app_personnage_index')}}">Accueil Personnage
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{path('app_race_index')}}">Accueil Race
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				{% endif %}
			</ul>
<div class="d-flex">
				{% if is_granted('ROLE_ADMIN') %}
					<a class="btn btn-grad me-2" href="{{path('app_user_index')}}">user</a>
			
				{% endif %}
				{% if is_granted('ROLE_USER') %}
					<a class="btn btn-grad me-2" href="{{path('app_profile')}}">Profil</a>
					<a class="btn btn-grad me-2" href="{{path('app_logout')}}">Deconnexion</a>
				{% else %}

					<a class="btn btn-grad me-2" href="{{path('app_login')}}">Connexion</a>
					<a class="btn btn-grad me-2" href="{{path('app_register')}}">Inscription</a>
				{% endif %}
			</div>
		</div>
	</div>
</nav>
